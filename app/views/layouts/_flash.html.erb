<div class="container">
	<div class="row">
		<div class="col-md-8 col-md-offset-4">
<% flash.each do |type, message| %>
  <div id="flash" class="alert <%= bootstrap_class_for(type) %> fade in">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
  <% flash.clear %>
<% end %>
</div>
</div>
</div>